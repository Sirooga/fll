var counter = 1;

var printMessage = function() {
  console.log("you are looking into the console for " + counter + " seconds");
  counter++;
};

var intervalId = setInterval(printMessage, 1000);